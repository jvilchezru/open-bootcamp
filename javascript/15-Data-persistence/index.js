const name = "Jesus"
const lastname = "Vilchez"

const person = {
  name,
  lastname
}

// Session Storage
// sessionStorage.setItem("data-session", JSON.stringify(person))

// Local Storage
// localStorage.setItem("data-local", JSON.stringify(person))

// Cookie
// document.cookie = `dataCookie=${JSON.stringify(person)};expires=${new Date(new Date().getTime() + (2 * 60000)).toUTCString()}`

